diff --git a/lib/MahoCLI/Commands/Serve.php b/lib/MahoCLI/Commands/Serve.php
index 1170940..b6d8d6c 100644
--- a/lib/MahoCLI/Commands/Serve.php
+++ b/lib/MahoCLI/Commands/Serve.php
@@ -34,11 +34,22 @@ class Serve extends BaseMahoCommand
     #[\Override]
     protected function execute(InputInterface $input, OutputInterface $output): int
     {
-        $host = '127.0.0.1';
-        $port = $input->getArgument('port');
+        // Default to localhost for safety, but bind to all interfaces when running
+        // inside containers (Traefik/other containers must be able to reach us).
+        $host = getenv('MAHO_SERVE_HOST')
+            ?: (getenv('LARAVEL_maho') ? '0.0.0.0' : '127.0.0.1');
+
+        $port = (int) $input->getArgument('port');
         $docroot = MAHO_PUBLIC_DIR;
 
-        passthru("php -S {$host}:{$port} -t {$docroot}");
+        $address = $host . ':' . $port;
+        passthru(sprintf(
+            'php -S %s -t %s',
+            escapeshellarg($address),
+            escapeshellarg($docroot),
+        ));
 
         return Command::SUCCESS;
     }
 }
