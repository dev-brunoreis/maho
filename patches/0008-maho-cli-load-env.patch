diff --git a/maho b/maho
index 3d8f1aa..0af1888 100755
--- a/maho
+++ b/maho
@@ -14,6 +14,52 @@ define('MAHO_PUBLIC_DIR', __DIR__ . '/public');
 
 require __DIR__ . '/vendor/autoload.php';
 
+// Load .env early (credentials only) - no external dependency
+if (!function_exists('maho_load_env_file')) {
+    function maho_load_env_file(string $envFile): void
+    {
+        if (!is_file($envFile) || !is_readable($envFile)) {
+            return;
+        }
+
+        $lines = file($envFile, FILE_IGNORE_NEW_LINES);
+        if (!is_array($lines)) {
+            return;
+        }
+
+        foreach ($lines as $line) {
+            $line = trim($line);
+            if ($line === '' || str_starts_with($line, '#')) {
+                continue;
+            }
+            if (!str_contains($line, '=')) {
+                continue;
+            }
+
+            [$key, $value] = explode('=', $line, 2);
+            $key = trim($key);
+            $value = trim($value);
+            if ($key === '') {
+                continue;
+            }
+
+            // Strip optional quotes
+            if ((str_starts_with($value, '"') && str_ends_with($value, '"'))
+                || (str_starts_with($value, "'") && str_ends_with($value, "'"))
+            ) {
+                $value = substr($value, 1, -1);
+            }
+            $value = str_replace(['\\n', '\\r', '\\"', "\\'"], ["\n", "\r", '"', "'"], $value);
+
+            $_ENV[$key] = $value;
+            $_SERVER[$key] = $value;
+            @putenv($key . '=' . $value);
+        }
+    }
+}
+
+maho_load_env_file(MAHO_ROOT_DIR . '/.env');
+
 use MahoCLI\CommandDiscoverer;
 use Symfony\Component\Console\Application;
 
